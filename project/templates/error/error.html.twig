{% extends 'base.html.twig' %}

{% set show_layout = false %}

{% block title %}Error {{ code }}{% endblock %}

{% block head %}
    {{ parent() }}
    <meta name="robots" content="noindex, nofollow">
{% endblock %}

{% block body %}
    <div class="min-h-screen bg-[#ECEFCA] flex flex-col items-center justify-center px-4 py-8">
        <div class="bg-white rounded-lg shadow-lg p-8 max-w-2xl w-full text-center border border-[#547792]">
            <h1 class="text-4xl sm:text-5xl font-extrabold text-[#213448] mb-4">Error {{ code }}</h1>
            <p class="text-[#94B4C1] mb-6 text-sm sm:text-base">
                {% if code == 400 %}
                    La solicitud no ha podido ser procesada debido a un problema con los datos enviados.
                {% elseif code == 401 %}
                    Acceso no autorizado. Es necesario iniciar sesión para continuar.
                {% elseif code == 403 %}
                    Acceso denegado. No dispone de los permisos necesarios para visualizar esta página.
                {% elseif code == 404 %}
                    La página solicitada no ha sido encontrada.
                {% elseif code == 405 %}
                    Método no permitido. La operación solicitada no es válida en este contexto.
                {% elseif code == 408 %}
                    El servidor no ha recibido una respuesta a tiempo.
                {% elseif code == 429 %}
                    Se ha excedido el número de solicitudes permitidas. Por favor, inténtelo nuevamente más tarde.
                {% elseif code == 500 %}
                    Se ha producido un problema interno en el servidor. Estamos trabajando para solucionarlo.
                {% elseif code == 502 %}
                    El servidor ha recibido una respuesta inválida de un servicio externo.
                {% elseif code == 503 %}
                    Servicio no disponible temporalmente. Por favor, inténtelo nuevamente más tarde.
                {% elseif code == 504 %}
                    El servidor no ha podido responder a tiempo. Inténtelo de nuevo en unos momentos.
                {% else %}
                    Se ha producido un inconveniente inesperado. Lamentamos las molestias ocasionadas.
                {% endif %}
            </p>
            <a href="/" class="inline-block bg-[#547792] hover:bg-[#213448] text-white font-semibold py-2 px-4 rounded transition-colors duration-300">
                Volver al inicio
            </a>
        </div>
    </div>
{% endblock %}
